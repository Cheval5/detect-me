import React from 'react';

const  Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className="f3 link dim underline pa3 pointer black-60 relative z10">Sign Out</p>
        </nav>
    );
};

export default Navigation;